using UnityEngine;

public class ChangeColor : MonoBehaviour
{
    //[SerializeField] private Spawner m_Spawner;
    [SerializeField] private float _speed =1f;
    private Cube _SpawmdeCube;

    private void Start()
    {
        //_SpawmdeCube = Spawner.SpawnCube(m_Spawner);
    }

}
